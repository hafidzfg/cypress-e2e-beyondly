{
  "scripts": {
    "run:e2e": "npx cypress run --spec 'cypress/e2e/*.feature'",
    "lint": "eslint --fix \"cypress/**/*.js\"",
    "format": "prettier --check \"cypress/**/*.js\" \"cypress/**/*.json\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bfi-finance/web-test-automation.git"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^22.2.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.5",
    "@eslint/js": "^9.30.1",
    "cypress": "^14.5.1",
    "cypress-if": "^1.13.2",
    "eslint": "^9.30.1",
    "eslint-plugin-cypress": "^5.1.0",
    "prettier": "3.6.2"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/e2e/*.js"
  }
}
